# app/core/web_search.py

# from duckduckgo_search import DDGS

def search_web(query: str) -> dict:
    # Dummy implementation to avoid dependency issues for now
    # In a real production scenario, use a proper search API (e.g., Bing, Google, Serper)
    return {
        "summary": "Web search is currently disabled. Please rely on the internal knowledge base."
    }
